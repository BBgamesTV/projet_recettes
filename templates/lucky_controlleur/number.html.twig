{% extends 'base.html.twig' %}

{% block title %}Lucky Numbers
{% endblock %}

{% block body %}
	<style>
		.number-box {
			display: inline-block;
			width: 60px;
			height: 60px;
			line-height: 60px;
			text-align: center;
			margin: 5px;
			font-size: 24px;
			color: white;
			border-radius: 8px;
		}
        .example-wrapper { 
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
            text-align: center;
            border: solid 1px gray;
            box-shadow: 4px 4px 18px 10px;
            border-radius: 20px;
            padding: 15px;
        }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
	</style>
    <div class="example-wrapper">

        <h1>Bonjour
            {{ username }}
            👋</h1>
        <h2>Voici tes 10 nombres porte-bonheur :</h2>

		{% for number in numbers %}
			<div class="number-box" style="background-color: {% if number is even %}red{% else %}black{% endif %};">
				{{ number }}
			</div>
		{% endfor %}
	</div>
{% endblock %}

